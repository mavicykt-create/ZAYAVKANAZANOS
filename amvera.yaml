meta:
  environment: node
  version: "20"
build:
  preBuild:
    - npm install
  artifacts:
    - "**/*"
run:
  command: node server.js
  ports:
    - port: 8080
      protocol: HTTP
  env:
    - name: PORT
      value: "8080"
    - name: NODE_ENV
      value: production
